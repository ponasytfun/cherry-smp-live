# Staff Avatars

Staff images are loaded from these filenames:

- `realares.png` for RealAries
- `glitch.png`
- `cowz.png`

The current app already points to these public paths in `src/data/staff.ts`, and falls back to initials if an image cannot load.
